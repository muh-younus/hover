let div = document.querySelectorAll(".div");
let index =0;
let val;
for(let divs of div){

   val= divs[index];

   if (val == divs[index]){

    divs[index].innerText = "Muhammad Younus";
    
   }
   else if (val == divs[index]){

    divs[index].innerText = "Alina";
    
   }

    
}